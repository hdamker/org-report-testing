name: Test CAMARA Workflow

on:
  workflow_dispatch:
    inputs:
      test_param:
        description: 'Test parameter'
        required: false
        type: boolean
        default: false

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: Test Basic Functionality
        uses: actions/github-script@v7
        with:
          github-token: ${{ secrets.CAMARA_TOKEN || secrets.GITHUB_TOKEN }}
          script: |
            console.log('🧪 Testing workflow startup...');
            console.log('✅ Workflow started successfully');
            console.log(`📝 Test parameter: ${{ github.event.inputs.test_param }}`);
            
            // Test basic API access
            try {
              const response = await github.rest.repos.listForOrg({
                org: 'camaraproject',
                per_page: 1
              });
              console.log(`✅ API access working: Found ${response.data.length} repositories`);
            } catch (error) {
              console.log(`❌ API access failed: ${error.message}`);
            }
              